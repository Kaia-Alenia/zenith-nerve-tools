[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "alenia-bridge"
version = "1.1.0"
description = "Librería puente interna para Nerve y Zenith"
dependencies = [
]
